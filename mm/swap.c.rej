--- mm/swap.c
+++ mm/swap.c
@@ -701,7 +705,7 @@
 			__ClearPageLRU(page);
 			del_page_from_lru_list(page, lruvec, page_off_lru(page));
 		}
-
+		
 		list_add(&page->lru, &pages_to_free);
 	}
 	if (zone)
